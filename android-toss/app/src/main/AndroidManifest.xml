<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 안드로이드 11+ 는 다른 앱이 기본적으로 **안 보인다.** 토스 앱에 주문 화면을
         건네주려면 여기에 적어 둬야 하고, 안 적으면 setPackage 가 조용히 실패한다. -->
    <queries>
        <package android:name="viva.republica.toss" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuantDashboard">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.QuantDashboard">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 토스 주문 화면으로 넘어가기 위한 징검다리. 앱 전용 주소를 한 번 배우고 나면
             다시 뜨지 않는다. exported=false — 밖에서 부를 이유가 없다. -->
        <activity
            android:name=".TossOpenActivity"
            android:exported="false"
            android:label="토스로 이동"
            android:theme="@style/Theme.QuantDashboard" />
    </application>
</manifest>
